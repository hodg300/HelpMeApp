<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/helpmeicon"
    tools:context=".ListPlacesActivity">

    <TextView
        android:id="@+id/TitleCusLog"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:background="#E1E3EC"
        android:text="HelpMe"
        android:textAlignment="center"
        android:textSize="45dp"
        android:textStyle="bold|normal"
        tools:ignore="MissingConstraints"
        tools:layout_editor_absoluteX="0dp"
        tools:layout_editor_absoluteY="60dp" />

    <LinearLayout
        android:id="@+id/LinearCusLogIn"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:layout_below="@id/TitleCusLog"
        android:layout_above="@id/loginConfirmBtn"
        android:layout_centerInParent="true">

        <EditText
            android:id="@+id/customerEditName"
            android:layout_width="327dp"
            android:layout_height="66dp"
            android:layout_below="@id/TitleCusLog"
            android:background="@drawable/edit_round"
            android:layout_gravity="center"
            android:layout_marginTop="30dp"
            android:hint="Enter Your Name"
            android:inputType="textCapSentences"
            android:shadowColor="@android:color/white"
            android:textAlignment="center"
            android:textSize="30dp"
            android:textStyle="bold"
            />

        <TextView
            android:id="@+id/choose_place_textView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Choose your place"
            android:layout_marginTop="10dp"
            android:textSize="20dp"
            android:textAlignment="center"/>


        <ListView
            android:id="@+id/listView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
        </ListView>
    </LinearLayout>

    <Button
        android:id="@+id/loginConfirmBtn"
        android:background="@drawable/button_round"
        android:layout_centerInParent="true"
        android:layout_alignParentBottom="true"
        android:clickable="true"
        android:text="Get Help"
        android:layout_marginBottom="10dp"
        android:layout_width="120dp"
        android:layout_height="50dp"/>


</RelativeLayout>